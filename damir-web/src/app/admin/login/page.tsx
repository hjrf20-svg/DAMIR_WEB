import AdminLoginClient from '@/components/admin/AdminLoginClient'

export const metadata = {
  title: 'Acceso Admin — DAMIR',
}

export default function AdminLoginPage() {
  return <AdminLoginClient />
}
